app = "brandboda"
primary_region = "cdg"

[build]
  dockerfile = "Dockerfile"

# 🔥 تم حذف قسم [http_service] بالكامل
# كده Fly هيشغله كـ Worker ومش هيدور على بورت

[processes]
  app = "python3 -m BrandrdXMusic"

[[vm]]
  cpu_kind = "shared"
  cpus = 2
  memory_mb = 1024

[[swap]]
  size_mb = 512
